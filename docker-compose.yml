services:
  web:
    build: .
    container_name: soberania-digital
    ports:
      - 8080:8080
    environment:
      - PLANTAFORMAS_EMAIL=${PLANTAFORMAS_EMAIL}
      - PLANTAFORMAS_PASSWORD=${PLANTAFORMAS_PASSWORD}
